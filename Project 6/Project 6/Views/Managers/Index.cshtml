@model IEnumerable<Project_6.Models.RequestLeave>


@{
	ViewData["Title"] = "Index";
}


@* 
<p>
	<a asp-action="Create">Create New</a>
</p>

<h1>Leave Requests</h1>

<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Employee)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.RequestDate)
			</th>

			<th>
				@Html.DisplayNameFor(model => model.RequestDescription)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.StartTime)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.EndTime)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Status)
			</th>


		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			@if (item.Status == "Pending" && item.StartTime != null)
			{

				<tr>
					<td>
						@Html.DisplayFor(modelItem => item.Employee.Id)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.RequestDate)
					</td>

					<td>
						@Html.DisplayFor(modelItem => item.RequestDescription)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.StartTime)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.EndTime)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Status)
					</td>
					<td>

						<button asp-action="Approve" asp-route-id="@item.Id" type="submit" class="btn btn-success">Approve</button>
						<button asp-action="Reject" asp-route-id="@item.Id" type="submit" class="btn btn-danger">Reject</button>

					</td>
				</tr>
			}
		}
	</tbody>
</table>


<h1>Vecation Requests</h1>

<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Employee)
			</th>

			<th>
				@Html.DisplayNameFor(model => model.RequestDate)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.StartDate)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.EndDate)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Status)
			</th>



			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			@if (item.Status == "Pending" && item.StartTime == null)
			{

				<tr>
					<td>
						@Html.DisplayFor(modelItem => item.Employee.Id)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.RequestDate)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.StartDate)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.EndDate)
					</td>

					<td>
						@Html.DisplayFor(modelItem => item.Status)
					</td>



					<td>
						<a asp-action="Approve" asp-route-id="@item.Id" class="btn btn-success">Approve</a>
						<a asp-action="Reject" asp-route-id="@item.Id" class="btn btn-danger">Reject</a>

					</td>

				</tr>
			}
		}
	</tbody>
</table>







 *@





<div class="container" style="margin-top:80px ;">

	<h2>Leave Requests</h2>



	<table class="table table-bordered table-striped table-responsive-stack" id="tableOne">
		<thead class="thead-dark" >
			<tr style="background:#212529;">
				<th>Employee</th>
				<th>RequestDate</th>
				<th>RequestDescription</th>
				<th>StartTime</th>
				<th>EndTime</th>
				<th>Status</th>
				<th>Decision</th>

			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				@if (item.Status == "Pending" && item.StartTime != null)
				{
					<tr>
						<td>@item.EmployeeId</td>
						<td>@item.RequestDate</td>
						<td>@item.RequestDescription</td>
						<td>@item.StartTime</td>
						<td>@item.EndTime</td>
						<td>@item.Status</td>
						<td>
							<a asp-action="Approve" asp-route-id="@item.Id" class="btn btn-success">Approve</a>
							<a asp-action="Reject" asp-route-id="@item.Id" class="btn btn-danger">Reject</a>

						</td>
					</tr>
				}
			}


		</tbody>
	</table>


</div>





















<div class="container" style="margin-top:80px ;">

	<h2>Vecation Requests</h2>



	<table class="table table-bordered table-striped table-responsive-stack" id="tableOne">
		<thead class="thead-dark">
			<tr>
				<th>Employee</th>
				<th>RequestDate</th>
				<th>StartDate</th>
				<th>EndDate</th>
				<th>Status</th>
				<th>Decision</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				@if (item.Status == "Pending" && item.EndTime == null)
				{
					<tr>
						<td>@item.EmployeeId</td>
						<td>@item.RequestDate</td>
						<td>@item.StartDate</td>
						<td>@item.EndDate</td>
						<td>@item.Status</td>
						<td>
							<a asp-action="Approve" asp-route-id="@item.Id" class="btn btn-success">Approve</a>
							<a asp-action="Reject" asp-route-id="@item.Id" class="btn btn-danger">Reject</a>

						</td>
					</tr>
				}
			}


		</tbody>
	</table>


</div>
